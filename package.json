{
  "name": "add-colors-to-palette",
  "version": "1.3.0",
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "add-colors-to-palette",
    "manifest": "src/manifest.json",
    "main": "add-colors-to-palette.sketchplugin",
    "assets": [
      "assets/**/*"
    ]
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.5.16"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/johnmcclumpha/sketch-add-colors-to-palette.git"
  }
}
